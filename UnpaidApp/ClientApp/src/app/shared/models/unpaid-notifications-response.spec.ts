import { UnpaidNotificationsResponse } from './unpaid-notifications-response';

describe('UnpaidNotificationsResponse', () => {
  it('should create an instance', () => {
    expect(new UnpaidNotificationsResponse()).toBeTruthy();
  });
});
